{
  "name": "search",
  "version": "1.0.0",
  "scripts": {
    "start": "node run.js",  // <-- This overrides "node server.js"
    "dev": "node server.js"  // Optional (for local testing)
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
